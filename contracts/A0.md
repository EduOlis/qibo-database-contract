# Entregável B — Contrato Formal do Agente A0
## Agente de Extração de Evidências Clínicas

## 1. Definição Exata do Agente A0

### Identidade

**Nome:** Agente A0 — Extração de Evidências

**Natureza:** IA assistiva, não-autônoma

**Responsabilidade única:**
Identificar e sugerir trechos de texto brutos potencialmente relevantes para conhecimento clínico, sem interpretar ou estruturar entidades finais.

⚠️ **A0 não cria conhecimento, apenas aponta onde pode existir conhecimento.**

## 2. Posição no Pipeline

```
P0 (determinístico)
 ↓
kb_raw_chunks
 ↓
A0 (IA assistiva)
 ↓
kb_evidence_excerpts
 ↓
Gate humano obrigatório
```

**A0 nunca escreve diretamente em:**
- `kb_entity_proposals`
- `tcm_*`

## 3. Input (Entrada)

**Tabela lida:** `kb_raw_chunks`

**Campos utilizados:**
- `id` (uuid)
- `source_id` (uuid)
- `raw_text` (text)
- `language` (text)
- `page_reference` (text)
- `processed` (boolean)

**Pré-condições:**
- `processed = false`
- `raw_text` não vazio
- `source_id` válido

## 4. Output (Saída)

**Tabela escrita:** `kb_evidence_excerpts`

**Campos preenchidos pelo A0:**

- **`id`** (uuid, gerado)

- **`chunk_id`** (uuid → `kb_raw_chunks.id`)

- **`raw_text`** (text)
  🔒 Texto copiado literalmente, sem:
  - resumo
  - reescrita
  - limpeza semântica

- **`suggested_type`** (text)
  Valores possíveis (string livre, não enum):
  - `"symptom"`
  - `"clinical_sign"`
  - `"syndrome"`
  - `"acupoint"`
  - `"therapeutic_principle"`
  - `"mixed"`
  - `"uncertain"`

- **`relevance_score`** (numeric 0.00–1.00)

- **`justification`** (text)
  → explicação do porquê o trecho pode ser relevante, **não** interpretação clínica

- **`language`** (text)

- **`page_reference`** (text)

- **`status`** (text, default: `"pending"`)

- **`created_at`** (timestamp)

- **`created_by_agent`** (text, ex: `"A0-evidence-extractor"`)

## 5. Regras Estritas de Processamento

### 5.1 Preservação do Texto

- `raw_text` **DEVE** ser um substring literal de `kb_raw_chunks.raw_text`
- Nenhuma palavra pode ser:
  - alterada
  - resumida
  - completada
  - inferida
- Se o trecho não existir literalmente no chunk → **erro grave de contrato**

### 5.2 Escopo da IA

**O A0 PODE:**
- Identificar padrões linguísticos
- Reconhecer possíveis conceitos clínicos
- Sugerir tipo genérico (`suggested_type`)
- Estimar relevância relativa

**O A0 NÃO PODE:**
- Criar definições clínicas
- Inferir relações entre entidades
- Criar códigos, nomes normalizados ou estruturas
- Decidir validade clínica

## 6. Status e Workflow

**Campo `status`:**
- `"pending"` → padrão inicial
- `"approved"` → apenas humano
- `"rejected"` → apenas humano

⚠️ **A0 nunca altera `status` após criação**

## 7. Logs e Auditoria

**Tabela:** `kb_ingestion_logs` ou `mcp_audit_logs`

**Campos mínimos:**
- `operation = "A0_extraction"`
- `agent_name = "A0"`
- `agent_version`
- `input_chunks_count`
- `output_excerpts_count`
- `warnings`
- `execution_time_ms`

## 8. Invariantes do Sistema

### Invariantes de Integridade

- Todo `kb_evidence_excerpts.chunk_id` deve existir
- Um chunk pode gerar 0..N excerpts
- Um excerpt pertence a exatamente 1 chunk

### Invariantes de Segurança

- Nenhum dado clínico validado é criado
- Nenhuma entidade final é inferida
- Nenhuma relação é persistida

## 9. Idempotência e Reexecução

- A0 pode ser reexecutado
- Duplicação é permitida somente se explicitamente solicitada
- Idealmente:
  - chunks já processados recebem `processed = true`
  - ou controle por `kb_ingestion_logs`

## 10. O Que Explicitamente NÃO É Responsabilidade do A0

❌ Criar entidades clínicas
❌ Criar propostas estruturadas
❌ Aprovar ou rejeitar conteúdo
❌ Criar relações entre sintomas, síndromes e pontos
❌ Migrar dados
❌ Validar cientificamente qualquer trecho

### § 10.1 — Escopo de Atuação em Propostas de Entidade

O agente A0 (Evidence Extraction Agent) NÃO cria conhecimento clínico definitivo.
Sua função limita-se à extração de evidências literais e à proposição preliminar de entidades, quando aplicável.

Sempre que o A0 gerar uma proposta na tabela kb_entity_proposals, DEVE obedecer às seguintes regras:

**entity_type é obrigatório**

O A0 DEVE indicar o tipo da entidade clínica proposta (ex.: symptom, syndrome, acupoint, clinical_sign, etc.).

**entity_label é obrigatório quando a entidade for nomeável**

O valor de entity_label DEVE ser:

- um nome literal extraído do texto original, ou

- um rótulo mínimo e neutro, sem inferência clínica.

O A0 NÃO DEVE:

- normalizar nomes,

- traduzir termos,

- expandir siglas,

- inferir sinonímias,

- criar definições conceituais.

**proposed_data é opcional e restrito**

Quando presente, DEVE conter apenas:

- metadados estruturais,

- referências ao trecho literal,

- informações explicitamente declaradas no texto fonte.

É PROIBIDO incluir:

- definições clínicas,

- relações causais,

- interpretações diagnósticas.

### § 10.2 — Separação Clara de Responsabilidades

O A0 NUNCA:

- aprova propostas,

- rejeita propostas,

- altera o status de registros após a criação,

- revisa conteúdo próprio ou de outros agentes.

Qualquer alteração de:

- status,

- reviewed_by,

- reviewed_at,

- review_notes

É EXCLUSIVAMENTE responsabilidade de um revisor humano qualificado.

### § 10.3 — Garantia de Integridade

Toda proposta criada pelo A0:

- DEVE estar vinculada a um chunk_id existente,

- DEVE corresponder a um trecho literal previamente extraído,

- PODE ser rejeitada sem prejuízo ao histórico do sistema.

A rejeição de uma proposta NÃO implica erro do A0, apenas decisão editorial humana.

## 11. Exemplo Simplificado

**Input** (`kb_raw_chunks.raw_text`):
```
"A síndrome de Deficiência de Yin do Rim manifesta-se com sudorese noturna e boca seca."
```

**Output** (`kb_evidence_excerpts`):
```json
{
  "raw_text": "A síndrome de Deficiência de Yin do Rim manifesta-se com sudorese noturna e boca seca.",
  "suggested_type": "syndrome",
  "relevance_score": 0.91,
  "justification": "O trecho descreve explicitamente uma síndrome e seus sinais clínicos associados.",
  "status": "pending"
}
```

## 12. Ponto de Atenção (importante)

Se o A0 começar a produzir texto como:

> "Este texto descreve várias síndromes…"

👉 **Contrato violado.**
Isso é resumo, não evidência.
