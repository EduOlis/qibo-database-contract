# Contrato Formal do Agente A1
## Agente de Agrupamento Conservador de Evidências

## 1. Definição Exata do Agente A1

### Identidade

**Nome:** Agente A1 — Agrupamento Conservador de Evidências

**Modelo de Operação:** A1-α (conservador)

**Natureza:** IA assistiva, não-autônoma, estritamente dependente de aprovação humana prévia

**Responsabilidade única:** Agrupar evidências textuais previamente aprovadas por humanos, propor rótulos provisórios neutros e sinalizar possíveis conflitos ou redundâncias, sem realizar inferência clínica, normalização ou criação de conhecimento.

⚠️ **O A1 não cria conhecimento clínico. Ele apenas organiza evidências aprovadas para etapas posteriores.**

## 2. Dependência Obrigatória

O agente A1 depende integralmente do contrato A0 e de seus invariants de execução congelados.

**Referências obrigatórias:**
- `contracts/A0.md` (hash: `ab43bca68c1607c3b140bb72a60497d068a8ccd41145f29b00eedcbfbeab852c`)
- `contracts/a0_execution_invariants_v1.md` (hash: `dd34f985ae3ca215378af9550d702a18fbe846c4d2b1186455e8a824577480da`)

**Regras de dependência:**
- O A1 NÃO redefine responsabilidades do A0
- O A1 NÃO estende o escopo do A0
- O A1 NÃO reinterpreta decisões do A0
- Qualquer mudança futura em A0 invalida semanticamente este contrato

A invalidação semântica implica obrigatoriamente revisão formal deste contrato A1 e novo versionamento antes de qualquer execução futura do agente.

## 3. Posição no Pipeline

```
P0 (determinístico)
  ↓
kb_raw_chunks
  ↓
A0 (extração)
  ↓
kb_evidence_excerpts [status=pending]
  ↓
Gate humano obrigatório (aprovação)
  ↓
kb_evidence_excerpts [status=approved]
  ↓
A1 (agrupamento conservador)
  ↓
Estruturas intermediárias não finais
```

**O A1 nunca escreve diretamente em:**
- `tcm_*` (qualquer tabela final de conhecimento clínico)
- Tabelas de entidades clínicas finalizadas
- Tabelas de relações causais, terapêuticas ou diagnósticas

## 4. Input (Entrada)

**Tabela lida:** `kb_evidence_excerpts`

**Condições obrigatórias simultâneas:**

Todos os registros lidos pelo A1 DEVEM atender cumulativamente a:

1. `status = "approved"`
2. `reviewed_by IS NOT NULL` (aprovação humana registrada)
3. `reviewed_at IS NOT NULL` (timestamp de aprovação registrada)

**Cláusula de filtragem obrigatória:**

O A1 DEVE aplicar explicitamente, em TODAS as leituras de `kb_evidence_excerpts`, a seguinte cláusula SQL (ou equivalente funcional):

```sql
WHERE status = 'approved'
  AND reviewed_by IS NOT NULL
  AND reviewed_at IS NOT NULL
```

Qualquer leitura que não aplique explicitamente esta tripla condição constitui violação de contrato.

Implementações que realizem leitura de registros fora desta cláusula e apliquem filtragem equivalente posteriormente (por exemplo, em código de aplicação) são consideradas não conformes e caracterizam violação explícita deste contrato.

**Campos utilizados:**
- `id` (uuid)
- `chunk_id` (uuid)
- `excerpt_text` (text)
- `suggested_entity_type` (text)
- `relevance_score` (numeric)
- `justification` (text)
- `language` (text)
- `status` (text)
- `reviewed_by` (uuid)
- `reviewed_at` (timestamp)

**Restrições absolutas de entrada:**

O A1 explicitamente NÃO pode:
- Ler registros com `status = "pending"`
- Ler registros com `status = "rejected"`
- Inferir ou completar dados ausentes
- Acessar diretamente `kb_raw_chunks` para reprocessamento
- Reinterpretar decisões de aprovação/rejeição do A0 ou de humanos

## 5. Output (Saída)

**Natureza da saída:** Estruturas intermediárias não finais e não clínicas

**Tabelas de trabalho sugeridas:**
- `kb_evidence_clusters` (agrupamentos provisórios)
- `kb_provisional_labels` (rótulos neutros temporários)
- `kb_evidence_conflicts` (sinalizações de conflitos/redundâncias)

**Campos típicos de saída:**

### 5.1 Agrupamentos (`kb_evidence_clusters`)
- `cluster_id` (uuid, gerado)
- `evidence_ids` (array de uuid → `kb_evidence_excerpts.id`)
- `cluster_basis` (text: descrição textual da base do agrupamento)
- `provisional_label` (text: rótulo neutro e não normalizado)
- `confidence_level` (numeric, 0.00 – 1.00)
  - **Natureza do campo:** `confidence_level` NÃO representa validade clínica, relevância científica, força conceitual ou correção
  - Mede exclusivamente a consistência mecânica/computacional do agrupamento textual
  - NÃO deve ser interpretado como validação, julgamento de qualidade ou aprovação semântica
  - O cálculo de `confidence_level` DEVE ser determinístico, monotônico em relação à sobreposição textual literal, reproduzível dado o mesmo input textual bruto, e independente de pesos aprendidos, ajustes dinâmicos, heurísticas adaptativas ou qualquer forma de aprendizado de máquina.
  - O campo confidence_level NÃO pode ser utilizado para ordenação de relevância clínica, priorização de revisão, tomada de decisão automática ou qualquer forma de inferência downstream.
- `created_at` (timestamp)

### 5.2 Rótulos Provisórios (`kb_provisional_labels`)
- `label_id` (uuid, gerado)
- `label_text` (text: rótulo extraído ou mínimo)
- `evidence_ids` (array de uuid)
- `label_type` (text: descritivo, não ontológico)
- `created_at` (timestamp)

### 5.3 Sinalizações (`kb_evidence_conflicts`)
- `conflict_id` (uuid, gerado)
- `evidence_id_a` (uuid)
- `evidence_id_b` (uuid)
- `conflict_type` (text: "redundancy", "contradiction", "overlap")
- `description` (text)
- `created_at` (timestamp)

⚠️ **Todas as saídas do A1 são provisórias e requerem validação humana subsequente.**

## 6. Escopo Permitido

O agente A1 PODE realizar exclusivamente as seguintes operações:

### 6.1 Agrupamento por Similaridade Textual
- Agrupar evidências aprovadas por similaridade textual computacional, sem inferência clínica ou conceitual
- Identificar co-ocorrências textuais entre evidências
- Detectar sobreposições literais entre trechos aprovados

**Restrição:** Agrupamentos DEVEM ser baseados exclusivamente em características textuais observáveis e explícitas. Nenhuma inferência sobre significado clínico, equivalência conceitual ou relação semântica implícita é permitida.

**Regra explícita sobre similaridade textual computacional:**
- Similaridade textual computacional NÃO implica similaridade semântica, conceitual, clínica ou ontológica
- O A1 NÃO pode tratar textos como equivalentes com base em significado, apenas em padrões textuais observáveis
- Nenhuma medida de similaridade textual autoriza o A1 a inferir que dois conceitos são o mesmo

Para fins deste contrato, "tokens" referem-se exclusivamente a separações mecânicas baseadas em delimitadores explícitos (por exemplo: whitespace ou pontuação), sendo explicitamente proibida qualquer forma de tokenização estatística, subword, probabilística ou derivada de modelos treinados.

É explicitamente proibido o uso de vetorização estatística, embeddings, TF-IDF, BM25, cosine similarity ou qualquer técnica equivalente, mesmo que aplicada apenas como etapa intermediária de cálculo.

### 6.2 Proposição de Rótulos Neutros

Rótulos provisórios DEVEM ser:
- Extraídos literalmente do texto das evidências aprovadas, OU
- Compostos exclusivamente por concatenação literal de termos presentes nas evidências

O A1 DEVE:
- Manter registro explícito da fonte textual de cada rótulo proposto
- Marcar rótulos derivados de evidência única como "fonte única"
- Referenciar todas as evidências das quais o rótulo foi extraído

**Restrição:** Rótulos devem ser neutros, provisórios e não normalizados. É explicitamente proibido:
- Parafrasear termos
- Generalizar conceitos
- Abstrair ou sintetizar semanticamente
- Traduzir entre idiomas
- Aplicar normalização ontológica
- Enriquecer com conhecimento externo

### 6.3 Sinalização de Conflitos e Redundâncias
- Identificar evidências aprovadas com sobreposição textual significativa (redundância potencial)
- Identificar evidências aprovadas com conteúdo textualmente divergente sobre o mesmo termo (conflito potencial)
- Documentar co-ocorrências e padrões textuais observáveis

**Restrição:** Sinalizações devem ser descritivas e factuais, nunca resolutivas. O A1 não resolve conflitos, não julga qual evidência está correta e não decide qual deve prevalecer. Toda sinalização DEVE incluir a marcação "requer revisão humana".

### 6.4 Produção de Estruturas Intermediárias
- Criar agrupamentos temporários de evidências
- Gerar índices e metadados auxiliares
- Preparar dados para revisão humana ou processamento por agentes superiores (A2, A3, etc.)

**Restrição:** Estruturas intermediárias não são conhecimento clínico final e devem ser claramente marcadas como provisórias.

## 7. Escopo Proibido

O agente A1 explicitamente NÃO pode realizar as seguintes operações:

### 7.1 Criação de Entidades Clínicas Finais
❌ Criar registros em tabelas de entidades clínicas finalizadas
❌ Definir entidades como "válidas" ou "canônicas"
❌ Atribuir códigos de ontologias clínicas (ICD, SNOMED, etc.)

### 7.2 Inferência de Relações
❌ Inferir relações causais entre sintomas e síndromes
❌ Inferir relações terapêuticas entre pontos e condições
❌ Inferir relações diagnósticas entre sinais e doenças
❌ Criar grafos de conhecimento clínico

### 7.3 Normalização e Enriquecimento
❌ Normalizar terminologia clínica
❌ Traduzir conceitos entre idiomas ou sistemas
❌ Aplicar ontologias ou vocabulários controlados
❌ Enriquecer evidências com conhecimento externo
❌ Parafrasear ou reformular termos clínicos

### 7.4 Substituição de Julgamento Humano
❌ Aprovar ou rejeitar evidências
❌ Alterar o campo `status` de qualquer evidência
❌ Sobrescrever decisões de revisão humana
❌ Validar cientificamente qualquer conteúdo
❌ Resolver conflitos entre evidências

### 7.5 Acesso a Dados Não Aprovados
❌ Processar evidências com `status = "pending"`
❌ Processar evidências com `status = "rejected"`
❌ Reprocessar chunks originais de `kb_raw_chunks`
❌ Inferir dados ausentes ou incompletos

### 7.6 Inferência Semântica ou Conceitual
❌ Deduzir equivalências conceituais não explicitamente textuais
❌ Inferir que termos diferentes se referem ao mesmo conceito
❌ Aplicar raciocínio clínico para interpretar similaridade
❌ Usar conhecimento de domínio para agrupar evidências

## 8. Modelo de Falha Aceitável

O agente A1 adota o princípio de conservadorismo epistêmico:

### 8.1 Princípios de Falha
- **Falsos negativos são aceitáveis:** É preferível omitir um agrupamento do que criar um agrupamento incorreto
- **Omissão é preferível à inferência:** Na ausência de evidência textual explícita, o A1 DEVE abster-se de produzir saída
- **Incerteza deve ser explicitada:** Quando a confiança é baixa, o A1 DEVE sinalizar incerteza, nunca forçar conclusões

### 8.2 Critérios de Confiança
- Agrupamentos com confiança abaixo do threshold definido por perfil de execução devem ser sinalizados como "baixa confiança, requer revisão"
- Rótulos provisórios derivados de evidência única DEVEM ser marcados como "fonte única"
- Conflitos identificados com baixa certeza DEVEM incluir nota "possível conflito, requer revisão humana"

### 8.3 Regra de Abstenção
Em caso de dúvida sobre:
- A similaridade textual entre evidências
- A adequação de um rótulo extraído
- A natureza de um conflito ou redundância

O A1 DEVE abster-se de produzir saída ou produzir saída marcada como "incerta, requer revisão humana".

## 9. Regras Estritas de Processamento

### 9.1 Preservação de Rastreabilidade
- Todo agrupamento DEVE referenciar explicitamente os IDs das evidências fonte
- Todo rótulo provisório DEVE referenciar as evidências das quais foi derivado
- Toda sinalização DEVE referenciar as evidências envolvidas

### 9.2 Não-Destrutividade
- O A1 NUNCA altera registros em `kb_evidence_excerpts`
- O A1 NUNCA remove evidências aprovadas
- O A1 NUNCA modifica campos `status`, `reviewed_by`, `reviewed_at`

### 9.3 Transparência de Método
- Todo agrupamento DEVE incluir descrição textual da base do agrupamento (`cluster_basis`)
- Todo rótulo DEVE incluir referência à fonte textual de onde foi extraído
- Toda sinalização DEVE incluir descrição factual do conflito/redundância

### 9.4 Idempotência
- Múltiplas execuções do A1 sobre o mesmo conjunto de evidências aprovadas devem produzir resultados consistentes
- Agrupamentos não devem variar aleatoriamente entre execuções

## 10. Logs e Auditoria

**Tabela preferencial:** `mcp_a1_audit_logs`

O uso de tabelas genéricas é permitido apenas se for garantido isolamento lógico explícito por `agent_name`, `agent_model` e `profile_id`.

**Campos mínimos:**
- `operation = "A1_clustering"`
- `agent_name = "A1"`
- `agent_version`
- `agent_model = "A1-α"`
- `input_approved_excerpts_count`
- `output_clusters_count`
- `output_labels_count`
- `output_conflicts_count`
- `warnings`
- `execution_time_ms`
- `profile_id` (referência ao perfil de execução)

**Regra contratual:**

Toda execução do A1 DEVE gerar exatamente um registro de auditoria.

Execuções sem log são consideradas inválidas, mesmo que tenham produzido output.

Todo registro de log gerado pelo A1 DEVE conter explicitamente o campo `contract_version` com o valor exato `"A1 v1.1"`.

## 11. Invariantes do Sistema

⚠️ **Nota:** As invariantes descritas nesta seção são conceituais e definem propriedades esperadas do sistema A1. Elas NÃO substituem execution invariants formais, que serão definidos em artefatos separados e versionados.

### 11.1 Invariantes de Integridade
- Todo `evidence_id` referenciado pelo A1 DEVE existir em `kb_evidence_excerpts`
- Todo `evidence_id` processado DEVE ter `status = "approved"`
- Todo `evidence_id` processado DEVE ter `reviewed_by IS NOT NULL`
- Todo `evidence_id` processado DEVE ter `reviewed_at IS NOT NULL`

### 11.2 Invariantes de Segurança
- Nenhum dado clínico final validado é criado
- Nenhuma entidade clínica canônica é inferida
- Nenhuma relação causal, terapêutica ou diagnóstica é persistida
- Nenhuma normalização ontológica é aplicada
- Nenhuma inferência semântica ou conceitual é realizada

### 11.3 Invariantes de Rastreabilidade
- Todo output do A1 DEVE ser rastreável às evidências aprovadas de origem
- Todo agrupamento, rótulo ou sinalização DEVE incluir referências explícitas
- Nenhuma informação pode ser introduzida sem origem textual documentada

## 12. Limite Intencional do A1

O agente A1 possui limites intencionais e intransponíveis, definidos para garantir integridade epistemológica da base de conhecimento.

1. O A1 NÃO realiza validação científica, clínica, terapêutica ou epistemológica
2. O A1 NÃO julga correção, plausibilidade ou aderência a padrões clínicos
3. O A1 NÃO cria conhecimento clínico final
4. O A1 NÃO infere relações não explicitamente presentes nas evidências
5. O A1 NÃO normaliza ou traduz conceitos clínicos
6. O A1 NÃO interpreta similaridade semântica ou conceitual
7. O papel do A1 é estritamente organizacional e preparatório:
   agrupar por características textuais observáveis, rotular por extração literal e sinalizar padrões, sempre de forma conservadora.
8. Qualquer validação, interpretação final ou curadoria clínica é responsabilidade exclusiva de humanos ou agentes superiores (A2, A3, etc.).

**Este limite é deliberado.**
**Qualquer expansão deste escopo constitui violação de contrato.**

## 13. Exemplo Simplificado

### Input (kb_evidence_excerpts, status=approved):

**Evidência 1:**
```json
{
  "id": "uuid-001",
  "excerpt_text": "A síndrome de Deficiência de Yin do Rim manifesta-se com sudorese noturna e boca seca.",
  "suggested_entity_type": "syndrome",
  "status": "approved",
  "reviewed_by": "uuid-reviewer-1"
}
```

**Evidência 2:**
```json
{
  "id": "uuid-002",
  "excerpt_text": "Deficiência de Yin do Rim: sudorese noturna, boca e garganta secas.",
  "suggested_entity_type": "syndrome",
  "status": "approved",
  "reviewed_by": "uuid-reviewer-1"
}
```

### Output (kb_evidence_clusters):

```json
{
  "cluster_id": "uuid-cluster-001",
  "evidence_ids": ["uuid-001", "uuid-002"],
  "cluster_basis": "Sobreposição textual significativa: ambas contêm literalmente 'Deficiência de Yin do Rim' e 'sudorese noturna'",
  "provisional_label": "Deficiência de Yin do Rim",
  "confidence_level": "[definido por perfil de execução]"
}
```

### Output (kb_evidence_conflicts):

```json
{
  "conflict_id": "uuid-conflict-001",
  "evidence_id_a": "uuid-001",
  "evidence_id_b": "uuid-002",
  "conflict_type": "redundancy",
  "description": "Ambas evidências contêm o termo 'Deficiência de Yin do Rim' e listam sintomas sobrepostos. Possível redundância textual, requer revisão humana."
}
```

## 14. Ponto de Atenção Crítico

Se o A1 produzir saída como:

> "Este cluster representa a síndrome de Deficiência de Yin do Rim segundo a teoria da MTC, caracterizada por..."

👉 **Contrato violado.**
**Isso é interpretação clínica, não agrupamento conservador.**

Se o A1 criar um rótulo normalizado como:

> "Kidney Yin Deficiency (SNOMED: 12345678)"

👉 **Contrato violado.**
**Isso é normalização ontológica, não rótulo provisório neutro.**

Se o A1 inferir uma relação como:

> "A Deficiência de Yin do Rim causa sudorese noturna"

👉 **Contrato violado.**
**Isso é inferência causal, não sinalização de co-ocorrência textual.**

Se o A1 deduzir equivalência conceitual:

> "Deficiência de Yin do Rim é equivalente a Kidney Yin Deficiency"

👉 **Contrato violado.**
**Isso é inferência semântica, não identificação de sobreposição textual literal.**

## 15. Controle de Versão e Imutabilidade

**Versão:** A1 v1.1
**Data de criação:** 2026-01-27
**Status:** Ativo

**Dependências congeladas:**
- A0.md (hash: ab43bca68c1607c3b140bb72a60497d068a8ccd41145f29b00eedcbfbeab852c)
- a0_execution_invariants_v1.md (hash: dd34f985ae3ca215378af9550d702a18fbe846c4d2b1186455e8a824577480da)

O arquivo A1.md representa sempre a versão ativa mais recente do contrato A1. Versões anteriores existem apenas como registros históricos por meio de hashes congelados e não devem ser mantidas como arquivos paralelos.

**Regra de imutabilidade:**
Qualquer alteração nos artefatos A0 invalida semanticamente este contrato A1 e requer revisão formal.

---
## Hash de Congelamento

Algorithm: SHA-256
Computed-on: 2026-01-30T12:14:48Z
Hash: 5f4bf3ccb0e1bb9a622c133c9a6259692316ffa873c287e82a1770203b935d1c
---
